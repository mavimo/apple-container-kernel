---
name: Update kernel version 

on:
  workflow_dispatch:
  schedule:
    # Run once a day
    - cron: "30 0 * * *"

permissions:
  contents: write
  pull-requests: write

jobs:
  updatecli:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Install Updatecli in the runner
        uses: updatecli/updatecli-action@v3.3.0

      - name: Update Kernel 6.x Version
        run: updatecli apply --config .github/updatecli.d/update-kernel.yaml --values .github/updatecli.d/values.yaml --values .github/updatecli.d/values-6x.yaml
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Update Kernel 7.x Version
        run: updatecli apply --config .github/updatecli.d/update-kernel.yaml --values .github/updatecli.d/values.yaml --values .github/updatecli.d/values-7x.yaml
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
